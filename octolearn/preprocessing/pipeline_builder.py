# Preprocessing pipeline builder module
"""
PipelineBuilder module for Octolearn.

Provides utilities for building preprocessing pipelines (imputation, encoding, scaling, etc.).

Example:
	>>> from octolearn.preprocessing.pipeline_builder import PipelineBuilder
	>>> pipeline = PipelineBuilder(...)
	>>> X_clean = pipeline.fit_transform(X)
"""
